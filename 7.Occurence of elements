1. occurence of each elements in an array
def occurence(arr):
    d={}
    for i in arr:
        if i in d:
            d[i]+=1
        else:
            d[i]=1
    return d

print(occurence([1,1,2,2,3,3,3,4]))
